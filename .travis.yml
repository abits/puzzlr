sudo: required

language: go

services:
  - docker

install:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure

script:
  - go build -o /build/puzzlr .
  - docker build -t puzzlr/puzzlr .

deploy:
  provider: script
  script: bash docker-push.sh
